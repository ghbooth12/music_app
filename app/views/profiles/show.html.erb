<div class="row">
  <div class="col-md-8 col-md-offset-2">
    <div class="row">
      <div class="col-md-6">
        <img src="" alt="Image goes here" class="pull-right">
      </div>
      <div class="col-md-6">
        <h2>
          <%= @profile.user.username %>
          <small class="social-btn"><%= render 'list' %></small>
        </h2>
        <p>Jazz Pop Classic</p>
      </div>
    </div>

    <div class="row">
      <h3>About</h3>
      <p><%= @profile.body %></p>

      <div class="space-top"></div>

      <h3>Upcoming Concerts</h3>
      <% if @profile.user.concerts.any? %>
        <%= render partial: 'concerts/list', locals: { concerts: @profile.user.concerts } %>
      <% else %>
        <p>No upcoming concerts...</p>
      <% end %>

      <% if current_user && current_user == @profile.user %>
        <div class="space-top pull-right">
          <%= link_to 'Edit Public Profile', edit_user_profile_path(@profile.user, @profile), class: 'btn btn-primary' %>
        </div>
      <% end %>
    </div> <!-- /.row -->
  </div> <!-- /.col-md-8 -->
</div> <!-- /.row -->
